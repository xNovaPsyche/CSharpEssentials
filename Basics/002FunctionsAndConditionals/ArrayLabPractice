using System;

public class Program
{
	public void Main()
	{
		string[] playerTypes = {"1", "2", "3", "4"};
		
		for (var i = 0; i < playerTypes.Length ; i++) ;
		playerTypes[i] = "5";
	}
	
	foreach (var playerTypes in playerTypes) {
		Console.Writeline(playerType);
	}
}